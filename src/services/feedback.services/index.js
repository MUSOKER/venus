const {
  createFeedback, getFeedbacks, updateTheFeedback, deleteFeedback,
} = require('./feedback.services');

module.exports = {
  createFeedback, getFeedbacks, updateTheFeedback, deleteFeedback,
};
