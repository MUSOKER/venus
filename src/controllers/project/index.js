const {
    fetchAllProjects,
    filterProject,
    addProject,
    updateProject
} = require('./project.controllers');

module.exports = {
    fetchAllProjects,
    filterProject,
    addProject,
    updateProject
};